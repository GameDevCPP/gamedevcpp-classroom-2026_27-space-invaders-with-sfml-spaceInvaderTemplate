Place your own sprite sheet here (see README.md's "Resources for this
assignment/lab" section for a suggested source), e.g.:

    src/res/img/invaders_sheet.png

CMake copies this whole folder into your build output automatically,
so your code can load it at runtime as "res/img/invaders_sheet.png".
